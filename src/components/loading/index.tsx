import { Container } from "./styles";

// icone de carregamento de loading que aparece enquanto a pagina é carregada.
export const Loading = () => {
    return (
        <Container>
            <div className="loading"></div>
        </Container>
    );
};